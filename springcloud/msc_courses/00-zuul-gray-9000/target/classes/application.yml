server:
  port: 9000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka

spring:
  application:
    name: msc-zuul-depart

zuul:
  routes:
    msc-consumer-depart: /abc123/**