server:
  port: 8920

spring:
  application:
    name: msc-consumer-depart

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
  instance:
    metadata-map:
      host-mark: running-host
